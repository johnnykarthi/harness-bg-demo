apiVersion: v1
kind: Service
metadata:
  name: spring-app-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: spring-app
    # This value comes from values.yaml (either 'blue' or 'green')
    color: {{ .Values.trafficTo }}